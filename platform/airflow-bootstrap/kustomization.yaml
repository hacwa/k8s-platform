apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: airflow

resources:
  - external-secrets.yaml
  - postgresql-pvc.yaml
  - postgresql-service.yaml
  - postgresql-statefulset.yaml
  - platform-health-rbac.yaml
