apiVersion: v1
kind: Service
metadata:
  name: sound-buddies
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app.kubernetes.io/name: sound-buddies
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sound-buddies
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
