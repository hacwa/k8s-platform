apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jupyterhub-user-home
  namespace: jupyterhub
spec:
  accessModes:
    - ReadWriteMany

  storageClassName: nfs-csi

  resources:
    requests:
      storage: 100Gi
