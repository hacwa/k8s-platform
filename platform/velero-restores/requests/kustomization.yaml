apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Restore requests must be added here explicitly.
# The Argo CD application is manually synced and has no automated sync policy.
resources: []
