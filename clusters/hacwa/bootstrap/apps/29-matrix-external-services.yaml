apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: matrix-external-services
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "29"
spec:
  project: default
  source:
    repoURL: https://github.com/hacwa/k8s-platform.git
    targetRevision: main
    path: platform/external-services/matrix
  destination:
    server: https://kubernetes.default.svc
    namespace: external-services
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
