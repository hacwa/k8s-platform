apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: firefox
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "43"
spec:
  project: default

  source:
    repoURL: https://github.com/hacwa/k8s-platform.git
    targetRevision: main
    path: platform/firefox

  destination:
    server: https://kubernetes.default.svc
    namespace: firefox

  syncPolicy:
    automated:
      prune: false
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
