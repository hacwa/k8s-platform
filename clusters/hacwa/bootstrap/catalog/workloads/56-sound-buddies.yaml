apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sound-buddies
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "56"
spec:
  project: default
  source:
    repoURL: https://github.com/hacwa/k8s-platform.git
    targetRevision: main
    path: platform/sound-buddies
  destination:
    server: https://kubernetes.default.svc
    namespace: sound-buddies
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
